#Populate the list of repos required to scan under org or leave it blank or "*" to include all
include_repos:
  - ve*
 

#Populate the list of repos to skip from running the veracode the scan(s) under org or leave it blank or "*" to exclude all
exclude_repos:
    # - '*example*'
    # - 'exa*'
    - e*xa
    # - '*le*'
      # - '*'
    
  
  
